# PSE coursework — published pages

Built output only. The source lives in a private repository; this repo exists
so the pages can be served by GitHub Pages.

- `/`           landing page
- `/cw1/`       Prompt Engineer as a Career — CW1 group presentation
- `/pse/`       PSE Career Platform
- `/inflation/` Uzbekistan Inflation Dashboard

Regenerate with `./make-deploy.sh` in the source repository.
